# Orchestrator Custom Instructions

<!--
  Add your custom orchestrator instructions here.
  This file is yours — `metak install --force` will never overwrite it.

  Use this for standing rules specific to how the orchestrator should behave:
  - Planning preferences (e.g., "Always break frontend work into component-level tasks")
  - Review gates (e.g., "Always ask me to review architecture.md before spawning workers")
  - Repo creation rules (e.g., "Use Go for backend services, React for frontends")
  - Priority guidance (e.g., "API contracts must be finalized before any implementation tasks")
-->
